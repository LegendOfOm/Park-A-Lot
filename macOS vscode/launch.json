{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run SFML App (lldb)",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/sfml_app",
      "cwd": "${workspaceFolder}",
      "args": [],
      "externalConsole": true,
      "MIMode": "lldb",
      "stopAtEntry": false
    }
  ]
}
